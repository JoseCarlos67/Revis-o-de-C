// Faça um programa para ordenar um vetor com 100 números inteiros. Imprima o vetor antes e
// após a ordenação.

#include<stdio.h>
#include<stdlib.h>

void Organiza(int vetor[]) {
    int num = vetor[0];
    int aux = 0;
    for(int i = 1; i < 10; i++) {
        if(num > vetor[i]){
            aux = vetor[i]
        }
    }
}

int main(int argc, char const *argv[])
{
    
    return 0;
}
